// Genesis auto-generated: file_patch
// Patch a file: replace/insert/delete specific lines. Args: filepath, operation(replace/insert/delete), line_number, content(optional). For replace: replaces line N with content. For insert: inserts content after line N. For delete: removes line N.
#include <machina/plugin_api.h>
